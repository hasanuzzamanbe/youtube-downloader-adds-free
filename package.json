{
  "name": "youtube-downloader-frontend",
  "version": "1.0.0",
  "description": "Vue.js frontend for YouTube Downloader",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite preview --port 5000",
    "build:prod": "npm run build && echo 'Build complete! Run Flask app with: python app.py'"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
